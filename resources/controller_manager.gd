extends Node

var inventory: Array = [null, null, null, null, null, null, null, null, null, null]

var gold: int = 0
var silver: int = 0

'''func _ready() -> void:
	for key in EnemyManager.enemies:
		print(Util.ENEMY_TYPES.keys()[EnemyManager.enemies[key].enemy_type])'''
